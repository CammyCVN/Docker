version: '3.9'
services:
  app:
    build: .
    ports:
      - "8001:80"
    environment:
      RAILS_ENV: production
    depends_on:
      - db
      - redis

  sidekiq:
    build: .
    command: bundle exec sidekiq
    depends_on:
      - db
      - redis

  db:
    image: postgres:13
    environment:
      POSTGRES_USER: oav
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: oav_production

  redis:
    image: redis:6
